import { StyleSheet, Text, View } from "react-native";

const PickUp = () => {
    return (
        <View>
            <Text>This is PickUp screen component</Text>
        </View>
    );
};

export default PickUp;
